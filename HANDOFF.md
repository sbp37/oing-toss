# 인수인계 — 오잉게임

이 문서는 다른 작업자(사람이든 에이전트든)가 이 저장소를 이어받을 때 먼저 읽는
문서다. 지금 어디까지 왔는지, 다음에 뭘 해야 하는지, 그리고 **모르고 건드리면
깨지는 것들**을 적어둔다.

최종 갱신 기준: `main` = 새 오잉 카드 획득 연출, 앱 버전 1.1.0 (versionCode 7).

---

## 0. 먼저 할 일 — 최신본 받기

```sh
git fetch origin
git checkout main
git reset --hard origin/main
npm install
npm test          # 135개 전부 통과해야 정상 (약 60~110초)
```

로컬 브랜치가 뒤쳐져 있으면 그냥 버리고 `origin/main`을 기준으로 삼는다.
이 저장소는 PR을 squash 머지하므로, 오래된 작업 브랜치는 `main`과 히스토리가
갈라져 있어 rebase보다 새로 따는 쪽이 빠르다.

---

## 1. 이 게임이 뭔지

숫자 타일을 사각형으로 드래그해서 **합이 10이면 지우는** 퍼즐. 순수 웹
(바닐라 JS ES 모듈, 번들러 없음)이고, Capacitor로 감싸 안드로이드 앱으로도
나간다. 원스토어에 올라가 있다.

주 모드는 **클래식** — 2분, 판을 비우면 다음 판(더 큰 판)으로, 콤보 배율로
점수를 쌓는 스코어 어택. 홈의 시작하기가 이 모드다.

### 구조

| 파일 | 역할 |
| --- | --- |
| `js/game.js` | 게임 루프, 정답/실패 처리, 화면 전환 |
| `js/board.js` | 판 생성·정답 탐색(솔버)·제거 |
| `js/ui.js` | DOM 렌더링 전부 |
| `js/input.js` | 드래그 제스처 |
| `js/data.js` | 점수 공식·장면 정의 등 상수와 순수 함수 |
| `js/balance.js` | 스테이지 모드 밸런스 시뮬레이터 |
| `js/adapters.js` | localStorage·공유 등 바깥 세계와의 경계 |
| `css/play-layout-v1.css` | 플레이 화면 (덧붙이는 방식, 아래 규칙 참고) |
| `css/styles.css` | 그 외 전부 |

---

## 2. 모르고 건드리면 깨지는 것들

### 폰트 서브셋 가드 (제일 자주 걸린다)

`assets/fonts/*.woff2`는 **이 저장소에서 실제로 쓰는 한글만** 골라 담은
서브셋이다. `index.html`·`css/`·`js/`에 서브셋에 없는 한글이 하나라도 새로
들어가면 `npm test`가 실패한다. **주석에 쓴 글자도 포함된다.**

고치는 법은 둘 중 하나:

```sh
# (a) 서브셋을 다시 만든다 — 원본 폰트가 /tmp에 있어야 한다
python3 tools/build-jua-subset.py
python3 tools/build-pretendard-subset.py
```

원본이 없으면 `/tmp/Jua-Regular.ttf`, `/tmp/PretendardVariable.woff2`를 먼저
구해와야 한다. 없으면 (b) 주석 문구를 서브셋 안의 글자로 바꿔 쓴다.

### CSS는 덮어쓰지 말고 덧붙인다

`css/play-layout-v1.css`는 "패스"를 아래로 계속 덧붙여 온 파일이다. 위쪽
규칙을 고치는 대신 **파일 끝에 왜 바꾸는지 적은 주석과 함께 새 규칙을
추가**하는 것이 이 파일의 관례다. 히스토리가 이유와 함께 남는다.

### 판은 항상 "다 지울 수 있는" 상태로 생성된다

`js/board.js`의 생성기는 솔버로 검증한 판만 내보낸다. 지울 수 없는 칸(벽,
장애물 등)을 넣으려면 이 보장을 다시 설계해야 한다. 쉬워 보이지만 안 쉽다.

### 애니메이션 `fill-mode: both`

끝난 뒤에도 남아서 매 프레임 재계산·합성 레이어를 붙잡는다. 과거에 발열
원인이었고(#10, #11), 드래그 버벅임 원인이기도 했다(#24). 새 애니메이션을
넣을 때 `both`가 정말 필요한지 확인할 것.

### 입력 잠금

정답 처리 중 `state.inputLocked`가 켜지면 그 사이 들어온 pointerdown은
**지연되는 게 아니라 버려진다**. #25에서 잠금 구간을 최소화했으니
(칸 제거 직후 해제), 여기에 `await`를 다시 넣지 말 것.

---

## 3. 릴리스 (안드로이드)

### 개발 환경에서는 AAB를 못 만든다

이 저장소를 다루는 클라우드 컨테이너는 네트워크 정책상 `dl.google.com`이
막혀 있어 안드로이드 SDK와 AGP를 받을 수 없다. **빌드는 GitHub Actions에서
한다.**

- `.github/workflows/android-release.yml` — `main` 푸시 또는 수동 실행으로
  AAB/APK를 만들고 `android-build-<번호>` 프리릴리스로 올린다.
- 릴리스 자산은 `https://github.com/sbp37/oing-toss/releases/download/...`에서
  받을 수 있다 (이 호스트는 막혀 있지 않다).

### 서명

- 업로드 키는 **저장소에 없다.** 사용자가 `oing-upload.jks`로 로컬 보관한다.
- alias `oing-upload`, 인증서 SHA-256 지문
  `B3:86:85:F8:6E:8A:9F:80:69:FA:26:77:5E:BB:D7:DE:13:43:F5:65:B1:62:D5:A4:89:94:0B:79:4C:E2:7E:F6`
- **모든 빌드가 이 지문이어야 한다.** 다르면 스토어가 업데이트를 거부한다.
- CI에 시크릿 4개(`ANDROID_KEYSTORE_BASE64` 등)를 넣으면 CI가 서명까지 한다.
  안 넣으면 서명 없는 결과물이 나오고, 받아서 `jarsigner`로 직접 서명한다:

```sh
jarsigner -keystore oing-upload.jks -digestalg SHA-256 -sigalg SHA256withRSA \
  app-release.aab oing-upload
keytool -printcert -jarfile app-release.aab    # 지문 확인
```

- 원스토어 앱 서명은 PEPK 방식으로 **이미 등록 완료**했다. 다시 할 일 없다.
- 자세한 절차는 `store/ANDROID.md`, `store/ONESTORE-UPLOAD.md`.

### 버전

새 빌드를 올릴 때마다 `android/app/build.gradle`의 `versionCode`를 **반드시
1 올린다.** 스토어는 같은 번호를 두 번 받지 않는다.

### 절대 커밋하지 않는 것

`*.jks`, `*.keystore`, `android/keystore.properties`, `*.aab`, `*.apk`,
PEPK ZIP. `.gitignore`가 막고 있고 `tests/android-signing.test.mjs`가
그 사실을 테스트로 붙잡고 있다.

---

## 4. 도감(카드 수집) 시스템 — 구현 완료

친구들 플레이 피드백에서 나온 방향이다. 핵심은 **"잘하면 눈에 보이는 쾌감 +
실제 이득 + 쌓이는 수집물"**을 잇는 것.

### 지금 있는 것 vs 새로 만드는 것

**기존 장면 7장은 그대로 둔다.** 판 배경 겸 숨은그림이고, 그 판을 60%
비우면 해금된다(`CLASSIC_CHAPTER_COLLECT_RATIO`). 판과 그림이 붙어 있는
구조라 건드리지 않는 게 좋다.

다만 **비밀 장면 오로라 항구의 5,000점 문턱은 너무 낮다** — 보통 실력이 첫
판에 넘는다. `CLASSIC_SECRET_CHAPTER.minScore`를 **15,000**으로 올린다.

그 위에 **새 카드 9장**을 별도 계층으로 얹는다. 판이 아니라 "플레이 행동"으로
얻는 카드다.

### 실측 데이터 (해금 조건의 근거)

봇으로 실제 클래식 2분 판을 6회 돌린 판당 평균이다. 봇은 답을 완벽히 "보는"
대신 판단 속도·실수율만 사람처럼 뒀으므로, **각 속도대의 상한**에 가깝다.
진짜 사람은 이보다 낮게 나온다.

| 실력 | 점수 | 지운 칸 | 5칸+ 클리어 | 고양이 | 최고 콤보 | 도달 판 |
| --- | --- | --- | --- | --- | --- | --- |
| 초보 (3.4초/수, 실수 16%) | 2,479 | 154 | 11회 | 6.5 | 35 | 3.5 |
| 보통 (1.9초/수, 실수 7%) | 15,958 | 680 | 58회 | 20.5 | 108 | 10.5 |
| 고수 (1.05초/수, 실수 2%) | 56,814 | 2,090 | 238회 | 67.5 | 178 | 24.5 |

재현하려면 `npx serve -s . -l 8766` 후 Playwright로 `?test=1`에 붙어
`window.__OING_TEST__`(`startClassic`/`findAnswers`/`commit`/`getState`)로
돌리면 된다.

### 확정된 해금 조건 9장

점수로 잠기는 카드는 **딱 2장**이다. 점수는 실력 천장이라, 캐주얼한 사람은
아무리 오래 해도 못 넘을 수 있다. 나머지 7장은 **느려도 반드시 도달하는**
누적·출석 조건으로 둔다.

| # | 조건 | 초보 | 보통 | 고수 |
| --- | --- | --- | --- | --- |
| 1 | 첫 판 끝내기 | 첫날 | 첫날 | 첫날 |
| 2 | 10판 플레이 | 2~3일 | 2~3일 | 2~3일 |
| 3 | 고양이 100마리 (누적) | 15판 | 5판 | 1판 |
| 4 | 5칸 이상 한 번에 300번 (누적) | 27판 | 5판 | 1판 |
| 5 | **한 판 8,000점** | 실력 필요 | 첫 판 | 즉시 |
| 6 | 서로 다른 7일 플레이 | 7일 | 7일 | 7일 |
| 7 | 지운 칸 20,000개 (누적) | 130판 | 30판 | 10판 |
| 8 | 서로 다른 30일 플레이 | 한 달 | 한 달 | 한 달 |
| 9 | **한 판 15,000점** | 멀다 | 잘한 판 | 즉시 |

9번 30,000점은 봇 기준이라 사람 천장은 더 낮을 수 있다. 실제 기록이 쌓이면
조정한다 — 상수 하나다.

출석은 **연속(streak)이 아니라 "서로 다른 날 수"**로 센다. 하루 빠졌다고
초기화되면 캐주얼 게임에서 스트레스가 된다.

### 저장키

카드가 보는 누적값은 전부 `oing_toss_v3_*` 새 키라 기존 저장값을 건드리지
않는다. 카드에는 **획득 플래그가 없다**(위 참고). 연출 때문에 늘어난 키는
딱 하나다:

- `oing_toss_v3_rare_item_intro_seen` — 첫 등장 안내를 이미 본 희귀 아이템
  종류 목록(JSON 배열). "봤다"는 사실은 누적값에서 되짚을 수 없어서 기록이
  필요하고, 종류마다 키를 만들면 아이템이 늘 때마다 키가 늘어난다.

### 새로 세야 하는 값

지금 평생 누적되는 건 **고양이뿐**이다(`storageAdapter.addCatsRescued`).
`js/adapters.js`에 새 키를 추가한다 (기존 키는 건드리지 않는다):

**이 부분은 구현 완료됐다.** `js/adapters.js`에 키 다섯 개와 읽기/더하기
함수가 있고, `js/game.js`의 `commitLifetimeTotals()`가 판이 끝날 때 한 번에
올린다. 스테이지와 클래식 두 종료 지점에서 모두 부른다.

| 키 | 용도 |
| --- | --- |
| `oing_toss_v3_runs_played` | 2번 — 플레이한 판 수 |
| `oing_toss_v3_big_clears` | 4번 — 5칸 이상 클리어 누적 |
| `oing_toss_v3_cells_cleared` | 7번 — 지운 칸 누적 (폭탄이 함께 터뜨린 칸 포함) |
| `oing_toss_v3_play_days` | 6·8번 — 플레이한 날짜(`YYYY-MM-DD`) 목록 |
| `oing_toss_v3_classic_best_combo` | 클래식 최고 콤보 (기존 `best_combo`는 스테이지 모드 전용이다) |

주의할 점 둘:
- **테스트 모드(`?test=1`)에서는 저장하지 않는다.** 자동 실행이 사람의 기록을
  밀어내면 안 되기 때문이다. 저장 경로를 확인하려면 하네스 없이 진짜 한 판을
  돌려야 한다.
- 판 중간이 아니라 **판이 끝날 때 한 번에** 올린다. 중간에 저장하면 앱이
  죽었을 때 반만 쌓인 값이 남는다.

날짜는 기기 시계 + localStorage 기준이다. 앱 데이터를 지우면 초기화되고
기기마다 따로 쌓인다. 서버가 없으므로 이게 한계다 — 그대로 받아들인다.

### 필요한 그림 (사용자가 그린다)

**들어왔다.** 원본은 `assets/source/cards-v1/`(1086×1448 PNG, 배포본에서 제외),
구운 결과는 `assets/cards/`와 `assets/cards/thumbs/`에 있다.

파일명이 카드 key와 다른데, 그림을 그린 쪽의 이름을 그대로 두는 편이 나중에
원본을 찾기 쉬워서다. `js/data.js`의 `art` 값이 파일명을 가리킨다. 05와 09의
파일명에 남은 20000/30000은 문턱을 정하기 전에 붙은 이름이고, 그림 안에는
숫자가 없으므로 조건과 어긋나지 않는다.

새 그림을 더할 때 규격:

| 파일 | 규격 | 용도 |
| --- | --- | --- |
| 원본 PNG | 3:4 세로, 1086×1448 | `assets/source/cards-v1/`에 두고 파이프라인에 넣는다 |
| 배포 원본 | 자동 생성, 180KB 이하 | 카드를 눌렀을 때 한 장씩 |
| 썸네일 | 자동 생성, 14KB 이하 | 기록 창을 열 때 |

- 기존 장면들과 같은 파스텔 수채화 톤.
- 카드 위에 제목이 얹히므로 **아래쪽 1/5은 단순하게** 비워둔다.
- 썸네일은 코드에서 자동 생성한다(원본만 주면 된다).
- **뒷면 1장만 있어도 갤러리의 회색 "???" 칸이 바로 카드처럼 보인다.**
  그림은 한 장씩 들어오는 대로 켤 수 있게 만들 것 — 기존 장면이
  `hasArt` 플래그로 그렇게 돼 있으니 같은 방식을 따른다.

### 아직 구현 안 된 것 (이 순서로 진행)

1. ~~카운터 5개 추가~~ — 완료
2. ~~카드 정의(9장) + 조건 판정 + 갤러리 표시~~ — 완료
   (`OING_CARDS`/`oingCardRows` in `js/data.js`, `renderOingCards` in `js/ui.js`,
   기록 창에 "오잉 카드" 구역. 잠긴 칸은 조건과 진행도(96/300)를 함께 보여준다.)
3. ~~그림 넣기~~ — 완료. 아홉 장과 뒷면이 모두 들어왔다. 파이프라인은 그대로 쓴다: 원본을 한 폴더에 모아 파일명을
   카드 key(또는 `back`)로 맞춘 뒤:

   ```sh
   python3 tools/build-card-assets.py <원본폴더>
   ```

   두 크기가 자동으로 나온다. 목표 용량 안에 들어올 때까지 품질을 낮춰가며
   굽고, 어느 파일이 예산을 넘겼는지 알려준다.

   | 산출물 | 크기 | 예산 | 언제 받나 |
   | --- | --- | --- | --- |
   | `assets/cards/<art>.webp` | 1086×1448 | 180KB | 카드를 눌렀을 때 한 장씩 |
   | `assets/cards/thumbs/<art>.webp` | 300×400 | 14KB | 기록 창을 열 때 한꺼번에 |

   그 다음 `js/data.js`에서 그림이 준비된 카드의 `hasArt`를 `true`로,
   뒷면이 들어왔으면 `OING_CARD_BACK_READY`를 `true`로 바꾼다. **한 장씩
   들어와도 된다** - 나머지는 계속 회색으로 남는다.

   `tests/card-assets.test.mjs`가 용량과 "첫 화면에서 받지 않는지"를 지킨다.
4. ~~카드 획득 순간의 연출~~ — 완료. 아래 4-1 참고.

카드 해금은 **어디에도 "열림"을 저장하지 않는다.** 기록 창을 열 때마다 지금
누적값으로 다시 판정한다. 조건을 나중에 손봐도 이미 열린 카드와 어긋나지 않게
하려는 것이다.

### 4-1. 카드 획득 연출 (구현됨)

결과 화면에서 이번 판에 처음 열린 카드 한 장을 세운다. 뒷면이 먼저 서고
곧 뒤집힌다.

| 어디 | 무엇 |
| --- | --- |
| `js/data.js` | `newlyUnlockedOingCards(totals, previousKeys)` — 순수 함수, 차집합 |
| `js/game.js` | `snapshotUnlockedCards()`(판 시작) / `cardsUnlockedThisRun()`(판 종료) |
| `js/ui.js` | `playCardAward()` / `resetCardAward()` |
| `index.html` | `#result-card-award` |
| `css/claude-polish.css` | 패널·뒤집기·장면 알약 |

**이번 판 신규 판정은 저장하지 않는다.** 판이 시작될 때 열려 있던 카드 key를
메모리(`this.cardKeysAtRunStart`)에 담아두고, 끝날 때 지금 목록과의 차집합을
낸다. 카드별 획득 플래그를 저장하면 조건을 손보는 순간 갤러리와 결과 화면이
서로 다른 말을 하기 시작한다 — 3절의 원칙과 같은 이유다.

호출 순서가 중요하다: `commitLifetimeTotals()`(누적값 반영)와
`saveClassicBestScore()`가 **먼저** 돌고 그 다음에 `cardsUnlockedThisRun()`이
와야 이번 판의 점수·칸수가 판정에 들어간다. `finish()`와 클래식 종료 두
곳 모두 그 순서로 되어 있다.

타이밍: 진입 0.3초 뒤 뒷면 등장 → 0.76초에 뒤집기 시작(520ms) → 앞면이 90도를
넘는 **0.91초**에 실제 노출. 신기록 콘페티가 있는 판은 0.4초 뒤로 물린다.
`prefers-reduced-motion`이면 딜레이 없이 바로 공개한다.

그림은 격자에 깔던 **썸네일**(300×400, 11~13KB)만 쓴다. 원본은 눌러서 크게 볼
때만 받는 물건이고, 시작 화면 preload에는 아무것도 더하지 않았다.

여러 장이 한꺼번에 열리면 `OING_CARDS` 순서의 **마지막**(조건이 가장 무거운)
한 장만 크게 세우고 아래에 `외 N장`을 붙인다.

새 장면도 같은 판에 열렸으면 카드 아래 11px 알약 한 줄로 붙고, 이때 기존
`#result-chapter-earned`는 숨긴다. **카드 없이 장면만 열린 판은 패널이 뜨지
않고 기존 줄이 그대로 나온다** — 빈 카드 칸을 세우는 것보다 낫다는 판단이다.

### 4-2. 희귀 아이템 첫 등장 안내 (구현됨)

megabomb / freeze / clover가 보드에 처음 놓인 그 한 번만 기존 토스트로 1.6초
안내한다(`RARE_BOARD_ITEM_INTROS` in `js/data.js`).

**이 안내는 `game.js`의 `announceBoardItems()` 안에 있어야 한다.** 배치하는
자리(`placeBoardItems()`)로 옮기면 바로 뒤에 도는 기존 등장 문구
("메가폭탄 등장이다냥! 톡 눌러봐.")와 같은 순간에 겹치고, 판갈이 경로에서는
그 사이의 아이템 해금 토스트에 덮여 아예 보이지 않는다. 실제로 그렇게 만들었다가
되돌린 자리다.

같은 이유로 **"봤다"는 기록(`markRareItemSeen`)은 말이 실제로 나간 자리에서만**
남긴다. 놓자마자 적으면, 안내를 못 한 경로를 지나는 순간 기회만 잃는다.

말풍선이 아니라 토스트인 이유: 8줄 이상 깊은 판에서는 말풍선 자리가 접힌다
(`css/play-layout-v1.css`).

---

## 4-3. 스토어 제출 자료 현황

| 어디 | 상태 | 파일 |
| --- | --- | --- |
| 원스토어 | 업로드용 자료 완료 | `store/screenshots/*.jpg` (732×1300, 7장), `store/ONESTORE-UPLOAD.md` |
| 앱인토스(토스 웹앱) | 자료 완료 | `store/toss/` — 아이콘 600×600, 썸네일 1932×828, 세로형 636×1048 3장 |

- 토스 썸네일은 원스토어에 올린 **그래픽 이미지 원본**을 그대로 쓴다
  (`store/toss/source/graphic-source.webp`). 다시 뽑으려면
  `python3 tools/build-toss-thumbnail.py`.
- 꾸러미: `npm run build && npx ait build` → `oing-game.ait`
  (`apps-in-toss.config.mjs`가 `webBundleDir`을 `dist/client`로 가리킨다).
  `.ait`는 빌드 산출물이라 저장소에 두지 않는다.
- **아직 안 쓴 것: 앱 설명 문구.** 두 스토어 모두 필요하다.

---

## 5. 그 다음 (합의된 방향, 미착수)

- **WOW 점수 재조정**: 지금 5칸 이상 보너스는 `(칸수-4)×10` **고정값**이라
  콤보 배율을 안 탄다. 후반에는 사실상 의미가 없다. 배율에 태우고 콤보 증가도
  +2에서 +3으로 올리는 안. `js/data.js`의 `classicScoreForClear`.
- **아이템 지급 조건 변경**: 지금은 콤보 7마다 자동(`ITEM_REWARD_INTERVAL`).
  "5칸 이상 지우면 폭탄" 같은 **플레이어가 노릴 수 있는 조건**으로 바꾸면 큰
  묶음을 만들 이유가 생긴다.
- **판마다 한 줄 규칙** (예: "이번 판 고양이 2배): 판갈이 연출 자리에 한 줄로.
  한 번에 하나만 — 규칙을 겹치면 복잡해진다.
- **벽/장애물**: 판 생성 보장(2절)을 다시 설계해야 해서 마지막에.

---

## 6. 알려진 문제

- 8칸 깊은 판에서 말풍선이 보드 아래와 겹치는 문제는 말풍선을
  숨기는 것으로 해결했다(#23). 320dp 폭 구형 기기는 아직 확인 안 됨.
- 드래그 성능은 #24에서 낭비를 걷어냈지만, 남은 비용은 판 자체의 무게
  (칸마다 그라데이션·여러 겹 그림자·숨은그림 이미지)에서 온다. 더 줄이려면
  보이는 것을 바꿔야 한다.
- **숫자판과 배경의 구분은 해결했다.** `css/play-layout-v1.css` 마지막의
  `Crisp board grooves and steady idle paint` 패스가 푸른 회색 판 바닥을 칸 사이에
  직접 드러내고, 숫자 타일에는 1px 외곽선과 blur 없는 2px 높이만 남긴다.
  지워진 칸도 blur 없는 안쪽 1px 선만 써서 숨은 그림이 흐려지지 않는다. 숫자별
  색을 다시 나누지 않아 기존 난도와 단일 젤리 계열은 유지된다. 힌트·성공·실패
  효과만 짧게 작은 그림자를 쓴다. 360×780 / 390×844 / 430×932의 6×8 판에서
  타일은 53~63px 정사각형, 가로·세로 넘침 없음으로 확인했다. `sw.js`는 고정
  파일명의 CSS/JS만 network-first로 읽어 모바일에 이전 화면이 남지 않게 한다.

---

## 7. 작업 관례

- 커밋·PR 설명은 **한국어**로, "무엇을 왜"가 드러나게.
- 코드 주석도 한국어로 쓰되, 서브셋에 없는 글자를 조심한다(2절).
- 게임 규칙이나 밸런스를 바꿀 때는 **먼저 측정**한다. 봇으로 실제 판을
  돌려보는 것이 감보다 정확하다 — 이 문서의 표가 그렇게 나왔다.
- `npm test`는 135개 전부 통과해야 한다. 새 기능에는 가드 테스트를 붙인다.
