# 새 점수 기준 작업본 — 2026-09-05

브랜치: `codex/astra-ranking-rewards-v1`. 기존 미커밋 작업을 보존했다. 운영 배포와 서버 데이터 변경은 하지 않았다. 사용자 추가 요청으로 아래 AIT를 생성했다.

## AIT 인계 / release-check

- 패키지 검사: **PASS**. 공식 `ait build` 성공, ZIP 내부 CRC 이상 없음, 최신 규칙 버전/보너스/답 보기/기록 보존/UI 첫 목표와 실제 API 주소 확인.
- 파일: `output/ait/new-oing-score-v2-20260905-1154.ait`, 9,802,718 bytes.
- SHA-256: `c305dad4ea1f8f0c5b84b95e4228e4c9636b58c2c9d68eba42e13c2dedb88c05`.
- deploymentId: `01a06f7d-661f-7ad5-9b36-518254913ca5` (패키지 빌드 식별자이며 운영 게시 완료가 아님).
- 운영 공개 검수: **NEEDS FIX**. ① 서버 변경 미배포: 현재 운영에서 친구/100위 밖 내 순위는 기존 동작이다. 운영 API의 친구 목록/상위100 밖 계정으로 재현 가능하며 신규 서버 코드 배포와 통합 확인 필요. ② 기존 보드 생성 성능 검사 기준 초과: `node --test tests/board.test.mjs`, 마지막 9×7/round10 구간에서 재현. 자세한 측정은 아래에 기록했다.
- 토스 테스트 업로드용 파일 제공은 가능하다. 전체 변경이 운영에 반영됐다고 간주해 공개하는 것은 보류한다.

## 반영

- 점수 규칙 버전 2. 새 로컬 최고점수·최근 7판·최고 콤보를 별도 키에 저장한다. 기존 점수 키와 수집·사탕 데이터는 그대로 둔다.
- 첫 플레이 전 메인/카운트다운/플레이 목표는 1,000점. 첫 기록 이후 메인은 '이번 버전 최고'. 결과 비교는 새 최고기록만 사용한다.
- 내오잉에 '이전 버전 기록 보기'. 카드/장면의 기존 점수 해금은 이전/새 버전 최고값을 사용해 유지한다.
- 판 돌파 보너스: `round(min(340, 100 + (판-1)*40) × 지운비율² / 10)*10`, 완전히 비우면 60점 추가. 판 번호만 빠르게 넘기는 전략을 과하게 보상하지 않는다.
- 3/5/7판 통합 축하 연출. 큰 모달이나 다음 판 버튼 없이 기존 판 전환 흐름에 통합했다.
- 콤보 실패 감점을 25 이하 부분 70%, 초과 부분 50%로 적용해 25→26 경계의 역전 손해를 없앴다. 콤보 점수 증가율/시간 아이템 확률은 바꾸지 않았다.
- 결과 시트의 '남은 답 보기'와 복귀. 타이머/점수는 재개하지 않는다. 큰 보드도 별도 복귀 패널과 겹치지 않는다.
- 실제 등록 상태별 안내와 재시도. 별명 팝업은 플레이 전부터 이미 저장됐다고 말하지 않는다.
- 서버 코드: 친구도 누적 최고기록 사용, 상위 100명 밖의 내 순위 별도 계산. 서버 배포 전에는 운영에 적용되지 않는다.
- 온라인 랭킹의 기존 점수는 삭제/리셋하지 않았다. 이번 작업의 기록 분리는 기기 내 개인 기록이며, 새 제출에 규칙 버전을 남긴다.

## 검증

- 관련 자동 테스트 97개 통과. `npm run build`, JS 구문 검사, `git diff --check` 통과.
- 실제 브라우저 드래그로 점수 증가 확인. 세 판 진행에서 보너스 누적/총점 증가 확인.
- 360×780, 390×844, 430×932에서 플레이와 큰 보드 답 보기 확인. 가로 넘침 없음, 보드와 복귀 패널 겹침 없음. 복기 중 running=false, 결과 시트 inert=true, 복귀 정상.
- 예전 20,000점과 새 1,400점을 별도로 저장/표시하는 사례 검증. 이 숫자는 QA 세션에 넣은 예시이며 운영 기록이 아니다.
- 캐시 버전이 붙은 `dist/client`도 검수했다. 소스 개발 서버의 고정 서비스워커 캐시는 이전 모듈을 반환할 수 있으므로 배포 검증은 빌드 산출물로 해야 한다.
- 테스트용 카운트다운 생략 함수에 남던 커튼도 닫도록 수정했다. 실제 게임 카운트다운은 유지했다.
- 소규모 모의 플레이 3회/프로필: 초보 평균 약 1,245, 보통 4,184, 숙련 10,236점. 보너스 비중 약 10%. 근사 모델과 적은 표본이므로 실제 유저 점수 분포를 보장하지 않는다.

## 배포 전 남은 확인

- 전체 회귀 검사에서 기존 `tests/board.test.mjs` 성능 상한 실패: 9×7/round10 최대 162.1ms(기준 150ms). 해당 구간만 같은 seed로 재검사 시 중앙값 28.4ms, 최대 159.7ms. `js/board.js`와 이 테스트는 이번에 변경하지 않았다. 기준을 느슨하게 바꾸지 않았다. 실기기 부하 검수/성능 확인 필요.
- 운영 Firebase 통합 및 토스 실기기 검수는 아직 수행하지 않았다. AIT 생성은 완료됐지만 운영 배포 완료라고 안내하지 않는다.

## 캡처

- `output/playwright/astra-home-final.png`: 새 버전 첫 목표.
- `output/playwright/astra-legacy-record.png`: 이전/새 기록 분리 예시.
- `output/playwright/astra-breakthrough-v2-final.png`: 실제 3판 돌파 중간 프레임(애니메이션만 캡처용 일시 정지).
- `output/playwright/astra-play-v2-{360,390,430}.png`: 플레이 크기별 검수.
- `output/playwright/astra-late-answers-v2-{360,390,430}.png`: 큰 보드 답 보기.
